---
phase: 04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/MasterclassLayout.astro
  - src/components/PricingCta.astro
  - src/components/TargetAudienceSection.astro
  - src/components/WhatsIncludedSection.astro
  - src/components/LearningOutcomes.astro
  - src/components/CurriculumSection.astro
autonomous: true
requirements: [P4-CSS, P4-COMPONENTS, P4-TYPEFIX]

must_haves:
  truths:
    - "Shared animation CSS (decor-anim, decor-sway, bulb-glow, bulb-ray, included-slide-left, included-slide-right, hotspot-btn, highlight-path) is defined exactly once in MasterclassLayout.astro"
    - "Four new components exist: TargetAudienceSection, WhatsIncludedSection, LearningOutcomes, CurriculumSection"
    - "None of the 4 new components contain their own <style> blocks — all CSS comes from MasterclassLayout"
    - "PricingCta accepts course='wreath' without TypeScript error"
    - "npm run build passes with zero errors"
  artifacts:
    - path: "src/layouts/MasterclassLayout.astro"
      provides: "Shared animation CSS block"
      contains: "decor-anim"
    - path: "src/components/TargetAudienceSection.astro"
      provides: "Target audience 2x2 card grid"
      contains: "interface Props"
    - path: "src/components/WhatsIncludedSection.astro"
      provides: "Whats included checklist + overlapping images"
      contains: "interface Props"
    - path: "src/components/LearningOutcomes.astro"
      provides: "Learning outcomes list + images"
      contains: "interface Props"
    - path: "src/components/CurriculumSection.astro"
      provides: "Curriculum timeline with zigzag/stacked layouts"
      contains: "interface Props"
    - path: "src/components/PricingCta.astro"
      provides: "Fixed course type union"
      contains: "'wreath'"
  key_links:
    - from: "src/layouts/MasterclassLayout.astro"
      to: "all masterclass pages"
      via: "global CSS classes"
      pattern: "style is:global"
---

<objective>
Consolidate shared animation CSS into MasterclassLayout and extract 4 reusable section components from the florarium reference page.

Purpose: Eliminate CSS duplication across 3 pages and create shared components so kartini/vinok pages can be rebuilt cleanly in Plan 02/03.
Output: MasterclassLayout with shared CSS, 4 new components, fixed PricingCta type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-RESEARCH.md

@src/layouts/MasterclassLayout.astro
@src/pages/stvorennya-florariumu.astro
@src/pages/kartini-zi-stabilizovanih-roslin.astro
@src/components/PricingCta.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate shared animation CSS into MasterclassLayout + fix PricingCta type</name>
  <files>src/layouts/MasterclassLayout.astro, src/components/PricingCta.astro</files>
  <read_first>
    - src/layouts/MasterclassLayout.astro (current state — 33 lines, no style block)
    - src/pages/stvorennya-florariumu.astro (lines 803-1094 — the style block to extract shared classes from)
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 653-931 — style block)
    - src/pages/vinok-zi-stabilizovanih-roslin.astro (lines 680-958 — style block)
    - src/components/PricingCta.astro (line 8 — course type union)
  </read_first>
  <action>
**Step A — Add shared animation CSS to MasterclassLayout.astro:**

After the closing `</Layout>` tag (line 33), add a `<style is:global>` block containing these shared classes and their keyframes. Copy these EXACTLY from the florarium page (lines 828-854 for decor animations, 882-897 for slide-in, 899-913 for bulb, 969-1050 for hotspots, and highlight-path from kartini lines 837-849):

```css
/* === Decorative botanical animations === */
@keyframes decorFloat {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}
@keyframes decorSway {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  33% { transform: translateY(-7px) rotate(2deg); }
  66% { transform: translateY(-11px) rotate(-1.5deg); }
}
@keyframes decorFloatMirror {
  0%, 100% { transform: scaleX(-1) translateY(0px); }
  50% { transform: scaleX(-1) translateY(-10px); }
}
.decor-anim {
  animation: decorFloat var(--dur, 7s) ease-in-out var(--del, 0s) infinite;
  will-change: transform;
}
.decor-sway {
  animation: decorSway var(--dur, 7s) ease-in-out var(--del, 0s) infinite;
  will-change: transform;
}
.decor-anim-mirror {
  animation: decorFloatMirror var(--dur, 7s) ease-in-out var(--del, 0s) infinite;
  will-change: transform;
}

/* === Slide-in for overlapping images === */
.included-slide-left {
  opacity: 0;
  transform: translateX(-60px);
  transition: opacity 0.8s ease-out, transform 0.8s ease-out;
}
.included-slide-left.revealed {
  opacity: 1;
  transform: translateX(0);
}
.included-slide-right {
  opacity: 0;
  transform: translateX(60px);
  transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  transition-delay: 0.2s;
}
.included-slide-right.revealed {
  opacity: 1;
  transform: translateX(0);
}

/* === Lightbulb glow === */
.bulb-glow { filter: drop-shadow(0 0 6px rgba(255, 215, 0, 0.4)); }
.bulb-ray { animation: bulbRayPulse 2s ease-in-out infinite; }
.bulb-ray:nth-child(2) { animation-delay: 0.15s; }
.bulb-ray:nth-child(3) { animation-delay: 0.3s; }
.bulb-ray:nth-child(4) { animation-delay: 0.45s; }
.bulb-ray:nth-child(5) { animation-delay: 0.6s; }
@keyframes bulbRayPulse {
  0%, 100% { opacity: 0.3; transform: scale(0.85); }
  50% { opacity: 1; transform: scale(1.15); }
}

/* === Hotspot buttons === */
.florarium-hotspot-container { position: relative; }
.hotspot-btn {
  position: absolute;
  transform: translate(-50%, -50%);
  width: 28px; height: 28px;
  border-radius: 50%;
  background: #F3237D;
  border: none; cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  z-index: 10;
  transition: transform 0.2s ease;
}
.hotspot-btn:hover { transform: translate(-50%, -50%) scale(1.15); }
.hotspot-btn::before, .hotspot-btn::after {
  content: ''; position: absolute; top: 50%; left: 50%;
  display: block; border: 1px solid rgba(243, 35, 125, 0.5);
  border-radius: 50%;
  animation: hotspot-ripple 2.6s cubic-bezier(0.25, 0.9, 0.2, 1) infinite;
}
.hotspot-btn::before { width: 140%; height: 140%; border-color: rgba(243, 35, 125, 0.6); }
.hotspot-btn::after { width: 100%; height: 100%; border-color: rgba(243, 35, 125, 0.4); animation-delay: 0.4s; }
@keyframes hotspot-ripple {
  0% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
  100% { transform: translate(-50%, -50%) scale(1.7); opacity: 0; }
}
.hotspot-btn .hotspot-tooltip {
  position: absolute; bottom: calc(100% + 10px); left: 50%;
  transform: translateX(-50%) scale(0.9);
  background: #003B1C; color: white;
  font-family: 'Tenor Sans', serif; font-size: 14px;
  padding: 6px 14px; border-radius: 8px; white-space: nowrap;
  opacity: 0; pointer-events: none;
  transition: opacity 0.2s ease, transform 0.2s ease; z-index: 20;
}
.hotspot-btn .hotspot-tooltip::after {
  content: ''; position: absolute; top: 100%; left: 50%;
  transform: translateX(-50%); border: 6px solid transparent; border-top-color: #003B1C;
}
.hotspot-btn.active .hotspot-tooltip {
  opacity: 1; transform: translateX(-50%) scale(1); pointer-events: auto;
}

/* === Animated green highlight underline === */
.highlight-path {
  stroke-dasharray: 1200;
  stroke-dashoffset: 1200;
  opacity: 0;
  transition: stroke-dashoffset 1s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.3s ease-out;
  transition-delay: 0.3s;
}
.revealed .highlight-path,
.reveal.revealed .highlight-path {
  stroke-dashoffset: 0;
  opacity: 1;
}

/* === Curriculum image hover zoom === */
.curriculum-img {
  transition: transform 0.4s ease;
  will-change: transform;
}
.curriculum-img:hover {
  transform: scale(1.04);
}

/* === Gallery (used on kartini/vinok) === */
.gallery-img {
  transition: transform 0.5s ease, box-shadow 0.5s ease;
  will-change: transform;
}
.gallery-item:hover .gallery-img {
  transform: scale(1.07);
  box-shadow: 0 16px 40px rgba(0, 59, 28, 0.22);
}
.gallery-overlay {
  transition: background-color 0.4s ease;
}

/* === Reduced motion === */
@media (prefers-reduced-motion: reduce) {
  .decor-anim, .decor-sway, .decor-anim-mirror { animation: none !important; }
  .hotspot-btn::before, .hotspot-btn::after { animation: none !important; }
  .highlight-path { stroke-dashoffset: 0 !important; opacity: 1 !important; transition: none !important; }
  .bulb-ray { animation: none !important; }
  .included-slide-left, .included-slide-right { opacity: 1 !important; transform: none !important; }
  .curriculum-img, .gallery-img { transition: none !important; }
  .gallery-overlay { transition: none !important; }
}
```

**Step B — Fix PricingCta.astro course type:**

On line 8 of `src/components/PricingCta.astro`, change:
```
course: 'florarium' | 'paintings';
```
to:
```
course: 'florarium' | 'paintings' | 'wreath';
```

Do NOT touch any other parts of the file.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "decor-anim" src/layouts/MasterclassLayout.astro returns 2 or more
    - grep -c "hotspot-btn" src/layouts/MasterclassLayout.astro returns 5 or more
    - grep -c "included-slide-left" src/layouts/MasterclassLayout.astro returns 2 or more
    - grep -c "bulb-glow" src/layouts/MasterclassLayout.astro returns 1 or more
    - grep -c "highlight-path" src/layouts/MasterclassLayout.astro returns 3 or more
    - grep -c "gallery-img" src/layouts/MasterclassLayout.astro returns 2 or more
    - grep "'wreath'" src/components/PricingCta.astro returns a match
    - grep "style is:global" src/layouts/MasterclassLayout.astro returns a match
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>MasterclassLayout has a single shared `<style is:global>` block with all shared animation CSS. PricingCta accepts 'wreath' course type. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract TargetAudienceSection component</name>
  <files>src/components/TargetAudienceSection.astro</files>
  <read_first>
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 194-269 — the TargetAudience section to extract)
    - src/pages/vinok-zi-stabilizovanih-roslin.astro (lines 194-269 — vinok variant for comparison)
    - src/pages/stvorennya-florariumu.astro (lines 120-200 — florarium variant for comparison)
    - src/components/ValueProps.astro (reference for Props interface pattern)
  </read_first>
  <action>
Create `src/components/TargetAudienceSection.astro` with this exact Props interface and structure:

```astro
---
interface Props {
  titleImage: string;
  titleImageAlt: string;
  subtitle: string;
  cards: Array<{
    image: string;
    imageAlt: string;
    title: string;
    description: string;
  }>;
  decorLeftImage?: string;
  decorRightImage?: string;
}
const { titleImage, titleImageAlt, subtitle, cards, decorLeftImage, decorRightImage } = Astro.props;
---
```

The HTML template should match the kartini page's Target Audience section (lines 195-269), which is the more standard version. Key elements:

1. Outer `<section>` with `class="relative bg-white px-[10px] py-[40px] md:px-8 md:py-[56px] lg:py-[72px] overflow-hidden"`
2. Optional decorative images: if `decorLeftImage` provided, render `<img>` with `class="hidden lg:block absolute pointer-events-none z-0 decor-sway"` and style `top: 5%; left: -3%; width: 210px; height: auto; opacity: 0.9; filter: blur(1px); --dur: 7s; --del: -2s;`. If `decorRightImage`, render with `class="hidden lg:block absolute pointer-events-none z-0 decor-anim"` and style `top: 2%; right: 1%; width: 160px; height: auto; --dur: 6s; --del: -1s;`
3. Inner `<div class="max-w-[1280px] mx-auto relative z-10">`
4. Title image centered: `<div class="flex justify-center mb-[8px] reveal">` with `<img>` using `titleImage`, `titleImageAlt`, `width="1024" height="460" class="w-full max-w-[320px] md:max-w-[420px] lg:max-w-[460px] h-auto" loading="lazy"`
5. Subtitle paragraph: `<p class="text-brand-dark/70 text-[16px] md:text-[18px] leading-[28px] text-center mb-[40px] max-w-[780px] mx-auto reveal">{subtitle}</p>`
6. Cards grid: `<div class="grid grid-cols-1 md:grid-cols-2 gap-[16px] md:gap-[20px]">` with `.map()` over `cards` array
7. Each card: `<div class="bg-white border border-[#E0E0E0] rounded-[20px] p-[20px] flex flex-col items-center text-center md:flex-row md:items-center md:text-left gap-[16px] md:gap-[20px] reveal">` containing image (`flex-shrink-0 w-[220px] h-[220px] md:w-[260px] md:h-[260px] object-contain`) + div with h3 + p

All `<img>` tags must have explicit `width` and `height` attributes. Card images: `width="800" height="800"`.

**CRITICAL: Do NOT add any `<style>` or `<style is:global>` block to this component.** All animation CSS (decor-anim, decor-sway) is already defined in MasterclassLayout. Components only use class names, never define them.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/TargetAudienceSection.astro exists
    - grep "interface Props" src/components/TargetAudienceSection.astro returns a match
    - grep "titleImage" src/components/TargetAudienceSection.astro returns a match
    - grep "cards" src/components/TargetAudienceSection.astro returns a match
    - grep "decorLeftImage" src/components/TargetAudienceSection.astro returns a match
    - grep 'border border-\[#E0E0E0\]' src/components/TargetAudienceSection.astro returns a match
    - grep 'width="800" height="800"' src/components/TargetAudienceSection.astro returns a match
    - grep -c '<style' src/components/TargetAudienceSection.astro returns 0 (no style blocks — CSS lives in MasterclassLayout)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>TargetAudienceSection.astro component exists with typed Props, 2x2 card grid, optional decor images, all img tags have width/height, zero style blocks. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Extract WhatsIncludedSection and LearningOutcomes components</name>
  <files>src/components/WhatsIncludedSection.astro, src/components/LearningOutcomes.astro</files>
  <read_first>
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 369-440 for WhatsIncluded, lines 460-530 for LearningOutcomes)
    - src/pages/stvorennya-florariumu.astro (lines 300-500 for comparison of WhatsIncluded / LearningOutcomes sections)
    - src/components/TargetAudienceSection.astro (reference for Props pattern just created in Task 2)
  </read_first>
  <action>
**Component A: WhatsIncludedSection.astro**

Create `src/components/WhatsIncludedSection.astro` with:

```astro
---
interface Props {
  titleImage: string;
  titleImageAlt: string;
  items: string[];
  bonusItem?: string;
  productImg1: string;
  productImg1Alt: string;
  productImg2: string;
  productImg2Alt: string;
}
const { titleImage, titleImageAlt, items, bonusItem, productImg1, productImg1Alt, productImg2, productImg2Alt } = Astro.props;
---
```

Structure (extracted from kartini lines 369-440):
1. Outer `<section class="relative bg-white px-[10px] md:px-8 py-[40px] md:py-[56px] lg:py-[72px] pb-[80px] overflow-hidden">`
2. Inner `<div class="max-w-[1280px] mx-auto">`
3. Animated lightbulb SVG (copy exact SVG from kartini lines 375-386 — the inline SVG with `bulb-glow` class), wrapped in `<div class="flex justify-start ml-[40px] md:ml-[60px] lg:justify-center mb-[-20px] reveal">`
4. Title image: `<div class="flex justify-center mb-[24px] reveal">` with `<img src={titleImage} alt={titleImageAlt} width="1024" height="460" class="w-full max-w-[320px] md:max-w-[420px] lg:max-w-[460px] h-auto" loading="lazy" />`
5. Checklist: `<div class="reveal"><ul class="space-y-[20px] px-[14px] md:px-0 mb-[24px]">` — map over `items`, each `<li>` with green circle checkmark (background `#7fac00`). If `bonusItem` is set, render it as the last item with a pink circle (background `#F3237D`) and a "Бонус" badge nearby.
6. Overlapping product images: `<div class="relative" style="min-height: 380px;">` with `productImg1` having `class="rounded-[30px] reveal included-slide-left" style="width: 86%; display: block;" width="800" height="800"` and `productImg2` with `class="rounded-[30px] absolute reveal included-slide-right" style="width: 60%; bottom: 70px; right: 0; box-shadow: 0 8px 30px rgba(0,0,0,0.1);" width="800" height="800"`

**CRITICAL: Do NOT add any `<style>` or `<style is:global>` block.** All animation CSS (bulb-glow, bulb-ray, included-slide-left, included-slide-right) is already defined in MasterclassLayout.

**Component B: LearningOutcomes.astro**

Create `src/components/LearningOutcomes.astro` with:

```astro
---
interface Props {
  titleImage: string;
  titleImageAlt?: string;
  items: string[];
  img1: string;
  img1Alt: string;
  img2: string;
  img2Alt: string;
  decorImg?: string;
  decorStyle?: string;
}
const { titleImage, titleImageAlt = 'Результати навчання', items, img1, img1Alt, img2, img2Alt, decorImg, decorStyle } = Astro.props;
---
```

Structure (extracted from kartini — the "Результати" section which has title image, bullet list, two overlapping images):
1. Outer `<section class="relative px-[10px] py-[40px] md:px-8 md:py-[56px] lg:py-[72px] overflow-hidden" style="background-color: #f8f8f8;">`
2. Inner `<div class="max-w-[1280px] mx-auto">`
3. Title image centered with reveal class
4. A layout: on mobile stacked, on desktop `lg:grid lg:grid-cols-2 lg:gap-[40px] lg:items-start`
5. Left: bullet list with green checkmark circles (same pattern as WhatsIncluded)
6. Right: overlapping images using `included-slide-left` / `included-slide-right` pattern with optional decor overlay (if `decorImg` provided, render absolutely positioned with `decorStyle` as inline style)

**CRITICAL: Do NOT add any `<style>` or `<style is:global>` block.** All animation CSS is already defined in MasterclassLayout.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/WhatsIncludedSection.astro exists
    - File src/components/LearningOutcomes.astro exists
    - grep "interface Props" src/components/WhatsIncludedSection.astro returns a match
    - grep "interface Props" src/components/LearningOutcomes.astro returns a match
    - grep "bonusItem" src/components/WhatsIncludedSection.astro returns a match
    - grep "decorImg" src/components/LearningOutcomes.astro returns a match
    - grep 'bulb-glow' src/components/WhatsIncludedSection.astro returns a match
    - grep 'included-slide-left' src/components/WhatsIncludedSection.astro returns a match
    - grep -c '<style' src/components/WhatsIncludedSection.astro returns 0 (no style blocks)
    - grep -c '<style' src/components/LearningOutcomes.astro returns 0 (no style blocks)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>WhatsIncludedSection and LearningOutcomes components exist with typed Props, matching existing HTML patterns, zero style blocks. Build passes.</done>
</task>

<task type="auto">
  <name>Task 4: Extract CurriculumSection component</name>
  <files>src/components/CurriculumSection.astro</files>
  <read_first>
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 539-632 — the Curriculum section with stacked layout)
    - src/pages/stvorennya-florariumu.astro (lines 500-700 — the Curriculum section with zigzag layout)
    - src/pages/vinok-zi-stabilizovanih-roslin.astro (lines 538-657 — vinok curriculum with bullets)
    - src/components/TargetAudienceSection.astro (reference for Props pattern)
  </read_first>
  <action>
Create `src/components/CurriculumSection.astro` with:

```astro
---
interface LessonItem {
  title: string;
  titleRest: string;
  description?: string;
  bullets?: string[];
  imageMobile: string;
  imageDesktop: string;
  imageAlt: string;
}
interface Props {
  titleImage: string;
  titleImageAlt: string;
  lessons: LessonItem[];
  layout?: 'zigzag' | 'stacked';
  timelineClass?: string;
  decorImage?: string;
}
const { titleImage, titleImageAlt, lessons, layout = 'stacked', timelineClass = 'curriculum-timeline-k', decorImage } = Astro.props;
---
```

Structure:
1. Outer section with cream bg: `<section class="relative bg-white px-[10px] py-[40px] md:px-8 md:py-[56px] lg:py-[72px] overflow-hidden">`
2. Title image centered with reveal
3. Timeline container: `<div class={`relative ${timelineClass}`}>` — the CSS class handles the vertical line via `::before`
4. For each lesson, render a lesson block. The `layout` prop controls:
   - `'stacked'`: Image always on LEFT, text on RIGHT on desktop (`lg:flex lg:items-start lg:gap-[48px]`). Desktop image in a `hidden lg:flex lg:w-[40%]` wrapper. Number circle at left edge with inline positioning.
   - `'zigzag'`: Odd lessons have image LEFT / text RIGHT, even lessons have text LEFT / image RIGHT. Number circle centered on desktop (`lg:absolute lg:left-1/2`).
5. Each lesson has: numbered circle badge (lime `#7fac00` bg, white text), title with `highlight-path` SVG underline on the first word (`title` prop), rest of title (`titleRest`), mobile image (visible below lg, hidden on lg where it's in the flex row), desktop image (hidden below lg), description text or bullet list.
6. If `description` is provided, render as `<p>` with `text-[#7b7b7b] text-[18px] leading-[28px] font-heading ml-[48px]`.
7. If `bullets` array is provided, render as `<ul class="space-y-[16px] ml-[48px] mt-[8px]">` with each bullet as `<li>` with small green checkmark circles (24px, background `#7fac00`).
8. Each img must have `width` and `height` attributes and `loading="lazy"`. Desktop images: `width="600" height="600" class="w-[380px] h-auto rounded-[20px]"`. Mobile images: `width="600" height="600" class="w-[280px] h-auto"`.

NOTE: The page-specific CSS for `.curriculum-timeline` (florarium) and `.curriculum-timeline-k` (kartini/vinok) stays on each page's own `<style is:global>` since the timeline positioning differs. The component just applies the class name via the `timelineClass` prop.

**CRITICAL: Do NOT add any `<style>` or `<style is:global>` block to this component.** All animation CSS (highlight-path, curriculum-img) is already defined in MasterclassLayout. Timeline-specific CSS stays on each page.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/CurriculumSection.astro exists
    - grep "interface Props" src/components/CurriculumSection.astro returns a match (inside LessonItem block or Props block)
    - grep "layout" src/components/CurriculumSection.astro returns a match
    - grep "zigzag" src/components/CurriculumSection.astro returns a match
    - grep "timelineClass" src/components/CurriculumSection.astro returns a match
    - grep 'highlight-path' src/components/CurriculumSection.astro returns a match
    - grep "bullets" src/components/CurriculumSection.astro returns a match
    - grep -c '<style' src/components/CurriculumSection.astro returns 0 (no style blocks — CSS lives in MasterclassLayout or page-specific styles)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>CurriculumSection component exists with typed Props (LessonItem with description/bullets variants, zigzag/stacked layout modes), matching existing HTML patterns, zero style blocks. Build passes.</done>
</task>

</tasks>

<verification>
- `cd D:/projects/Daria/wife-website && npm run build` passes with zero errors
- All 4 new component files exist in src/components/
- MasterclassLayout.astro contains a `<style is:global>` block with shared animation CSS
- PricingCta.astro course union includes 'wreath'
- No existing page is broken (florarium/kartini/vinok still build — components are new files, not yet imported)
- Zero `<style` blocks in any of the 4 new components: `grep -c '<style' src/components/{TargetAudienceSection,WhatsIncludedSection,LearningOutcomes,CurriculumSection}.astro` all return 0
</verification>

<success_criteria>
- 4 new Astro components created with typed Props interfaces
- None of the 4 components contain `<style>` blocks (Pitfall 1 from RESEARCH)
- Shared CSS consolidated in MasterclassLayout (single source of truth)
- PricingCta TypeScript type fixed
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-01-SUMMARY.md`
</output>
