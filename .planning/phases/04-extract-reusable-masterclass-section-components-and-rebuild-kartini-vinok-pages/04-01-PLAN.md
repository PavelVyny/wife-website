---
phase: 04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/MasterclassLayout.astro
  - src/components/PricingCta.astro
  - src/components/MasterclassHero.astro
  - src/components/TargetAudienceSection.astro
  - src/components/EducationalBlock.astro
autonomous: true
requirements: [P4-CSS, P4-COMPONENTS, P4-TYPEFIX]

must_haves:
  truths:
    - "Shared animation CSS is defined once in MasterclassLayout.astro, not duplicated per page"
    - "MasterclassHero component renders a full hero section with bg image, Daria photo, decorative elements, badge, title with zigzag SVG, subtitle, and CTA"
    - "TargetAudienceSection renders a title image, subtitle, 2x2 card grid with optional decor"
    - "EducationalBlock renders title image, heading with highlight-path SVG, product image with hotspots, benefits list, and quote image"
    - "PricingCta accepts course='wreath' without TypeScript error"
    - "npm run build passes with zero errors"
  artifacts:
    - path: "src/layouts/MasterclassLayout.astro"
      provides: "Shared animation CSS block"
      contains: "style is:global"
    - path: "src/components/MasterclassHero.astro"
      provides: "Hero section component"
      contains: "interface Props"
    - path: "src/components/TargetAudienceSection.astro"
      provides: "Target audience card grid"
      contains: "interface Props"
    - path: "src/components/EducationalBlock.astro"
      provides: "Educational section with hotspots"
      contains: "interface Props"
    - path: "src/components/PricingCta.astro"
      provides: "Fixed course type union"
      contains: "'wreath'"
  key_links:
    - from: "src/layouts/MasterclassLayout.astro"
      to: "all masterclass pages"
      via: "global CSS classes"
      pattern: "style is:global"
---

<objective>
Consolidate shared animation CSS into MasterclassLayout, fix PricingCta type, and extract 3 complex components (MasterclassHero, TargetAudienceSection, EducationalBlock) from the florarium reference page.

Purpose: Create a shared CSS foundation and the most complex reusable components. The florarium page is the SINGLE SOURCE OF TRUTH — all layout, styling, decorative positions, animations are extracted from it verbatim.
Output: MasterclassLayout with shared CSS, 3 new components with typed Props, fixed PricingCta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-RESEARCH.md

@src/layouts/MasterclassLayout.astro
@src/pages/stvorennya-florariumu.astro
@src/components/PricingCta.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate shared animation CSS into MasterclassLayout + fix PricingCta type</name>
  <files>src/layouts/MasterclassLayout.astro, src/components/PricingCta.astro</files>
  <read_first>
    - src/layouts/MasterclassLayout.astro (full file — 34 lines, no style block yet)
    - src/pages/stvorennya-florariumu.astro (lines 803-1079 — the full style block to extract shared classes from)
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 653-916 — style block for comparison)
    - src/components/PricingCta.astro (line 8 — course type union)
  </read_first>
  <action>
**Step A — Add shared animation CSS to MasterclassLayout.astro:**

After the closing `</Layout>` tag (line 33), add a `<style is:global>` block. Copy these shared classes EXACTLY from the florarium page's style block. These are classes used on ALL three pages and must be defined once:

```css
/* === Decorative botanical animations === */
@keyframes decorFloat {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}
@keyframes decorSway {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  33% { transform: translateY(-7px) rotate(2deg); }
  66% { transform: translateY(-11px) rotate(-1.5deg); }
}
@keyframes decorFloatMirror {
  0%, 100% { transform: scaleX(-1) translateY(0px); }
  50% { transform: scaleX(-1) translateY(-10px); }
}
.decor-anim {
  animation: decorFloat var(--dur, 7s) ease-in-out var(--del, 0s) infinite;
  will-change: transform;
}
.decor-sway {
  animation: decorSway var(--dur, 7s) ease-in-out var(--del, 0s) infinite;
  will-change: transform;
}
.decor-anim-mirror {
  animation: decorFloatMirror var(--dur, 7s) ease-in-out var(--del, 0s) infinite;
  will-change: transform;
}

/* === Slide-in for overlapping images === */
.included-slide-left {
  transform: translateX(-60px);
  transition: opacity 0.8s ease-out, transform 0.8s ease-out;
}
.included-slide-left.revealed {
  transform: translateX(0);
}
.included-slide-right {
  transform: translateX(60px);
  transition: opacity 0.8s ease-out, transform 0.8s ease-out;
  transition-delay: 0.15s;
}
.included-slide-right.revealed {
  transform: translateX(0);
}

/* === Lightbulb glow === */
.bulb-glow { filter: drop-shadow(0 0 6px rgba(255, 215, 0, 0.4)); }
.bulb-ray { animation: bulbRayPulse 2s ease-in-out infinite; }
.bulb-ray:nth-child(2) { animation-delay: 0.15s; }
.bulb-ray:nth-child(3) { animation-delay: 0.3s; }
.bulb-ray:nth-child(4) { animation-delay: 0.45s; }
.bulb-ray:nth-child(5) { animation-delay: 0.6s; }
@keyframes bulbRayPulse {
  0%, 100% { opacity: 0.4; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.15); }
}

/* === Hotspot buttons === */
.florarium-hotspot-container { position: relative; }
.hotspot-btn {
  position: absolute;
  transform: translate(-50%, -50%);
  width: 28px; height: 28px;
  border-radius: 50%;
  background: #F3237D;
  border: none; cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  z-index: 10;
  transition: transform 0.2s ease;
}
.hotspot-btn:hover { transform: translate(-50%, -50%) scale(1.15); }
.hotspot-btn::before, .hotspot-btn::after {
  content: ''; position: absolute; top: 50%; left: 50%;
  display: block; border: 1px solid rgba(243, 35, 125, 0.5);
  border-radius: 50%;
  animation: hotspot-ripple 2.6s cubic-bezier(0.25, 0.9, 0.2, 1) infinite;
}
.hotspot-btn::before { width: 140%; height: 140%; border-color: rgba(243, 35, 125, 0.6); }
.hotspot-btn::after { width: 100%; height: 100%; border-color: rgba(243, 35, 125, 0.4); animation-delay: 0.4s; }
@keyframes hotspot-ripple {
  0% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
  100% { transform: translate(-50%, -50%) scale(1.7); opacity: 0; }
}
.hotspot-btn .hotspot-tooltip {
  position: absolute; bottom: calc(100% + 10px); left: 50%;
  transform: translateX(-50%) scale(0.9);
  background: #003B1C; color: white;
  font-family: 'Tenor Sans', serif; font-size: 14px;
  padding: 6px 14px; border-radius: 8px; white-space: nowrap;
  opacity: 0; pointer-events: none;
  transition: opacity 0.2s ease, transform 0.2s ease; z-index: 20;
}
.hotspot-btn .hotspot-tooltip::after {
  content: ''; position: absolute; top: 100%; left: 50%;
  transform: translateX(-50%); border: 6px solid transparent; border-top-color: #003B1C;
}
.hotspot-btn.active .hotspot-tooltip {
  opacity: 1; transform: translateX(-50%) scale(1); pointer-events: auto;
}

/* === Animated green highlight underline === */
.highlight-path {
  stroke-dasharray: 1200;
  stroke-dashoffset: 1200;
  opacity: 0;
  transition: stroke-dashoffset 1s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.3s ease-out;
  transition-delay: 0.3s;
}
.revealed .highlight-path,
.reveal.revealed .highlight-path {
  stroke-dashoffset: 0;
  opacity: 1;
}

/* === Curriculum image hover zoom === */
.curriculum-img {
  transition: transform 0.4s ease;
  will-change: transform;
}
.curriculum-img:hover {
  transform: scale(1.04);
}

/* === Gallery (used on kartini/vinok) === */
.gallery-img {
  transition: transform 0.5s ease, box-shadow 0.5s ease;
  will-change: transform;
}
.gallery-item:hover .gallery-img {
  transform: scale(1.07);
  box-shadow: 0 16px 40px rgba(0, 59, 28, 0.22);
}
.gallery-overlay {
  transition: background-color 0.4s ease;
}

/* === Hero text/decor animations (florarium pattern — source of truth) === */
.hero-text-fade {
  opacity: 0;
  animation: heroTextFadeIn 0.8s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}
@keyframes heroTextFadeIn {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 1; transform: translateY(0); }
}
.hero-zigzag-path {
  stroke-dasharray: 1200;
  stroke-dashoffset: 1200;
  opacity: 0;
  animation: heroZigzagDraw 1s cubic-bezier(0.4, 0, 0.2, 1) 0.6s forwards;
}
@keyframes heroZigzagDraw {
  to { stroke-dashoffset: 0; opacity: 1; }
}
.hero-daria-slide {
  opacity: 0;
  animation: heroDecorSlideIn 1.2s cubic-bezier(0.22, 1, 0.36, 1) 0.3s forwards;
  --slide-x: 0px;
  --slide-y: 40px;
}
@keyframes heroDecorSlideIn {
  from { opacity: 0; transform: translate(var(--slide-x, 0), var(--slide-y, 0)); }
  to { opacity: 1; transform: translate(0, 0); }
}
.hero-decor-slide {
  opacity: 0;
  animation: heroDecorSlideIn 1.4s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

/* === CTA pulse animation === */
@keyframes ctaPinkPulse {
  0%, 100% { box-shadow: 0 8px 24px rgba(230, 51, 128, 0.35); }
  50% { box-shadow: 0 14px 44px rgba(230, 51, 128, 0.65), 0 0 0 8px rgba(230, 51, 128, 0.1); }
}
.hero-cta-pulse {
  animation: ctaPinkPulse 2.4s ease-in-out infinite;
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.hero-cta-pulse:hover {
  transform: scale(1.05) translateY(-2px);
  box-shadow: 0 18px 52px rgba(230, 51, 128, 0.75);
  animation-play-state: paused;
}

/* === Reduced motion === */
@media (prefers-reduced-motion: reduce) {
  .decor-anim, .decor-sway, .decor-anim-mirror { animation: none !important; }
  .hotspot-btn::before, .hotspot-btn::after { animation: none !important; }
  .highlight-path { stroke-dashoffset: 0 !important; opacity: 1 !important; transition: none !important; }
  .bulb-ray { animation: none !important; }
  .included-slide-left, .included-slide-right { opacity: 1 !important; transform: none !important; }
  .curriculum-img, .gallery-img { transition: none !important; }
  .gallery-overlay { transition: none !important; }
  .hero-text-fade, .hero-daria-slide, .hero-decor-slide { animation: none !important; opacity: 1 !important; }
  .hero-zigzag-path { animation: none !important; stroke-dashoffset: 0 !important; opacity: 1 !important; }
  .hero-cta-pulse { animation: none !important; }
}
```

**Step B — Fix PricingCta.astro course type:**

On line 8 of `src/components/PricingCta.astro`, change:
```
course: 'florarium' | 'paintings';
```
to:
```
course: 'florarium' | 'paintings' | 'wreath';
```
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "style is:global" src/layouts/MasterclassLayout.astro returns a match
    - grep -c "decor-anim" src/layouts/MasterclassLayout.astro returns 2 or more
    - grep -c "hotspot-btn" src/layouts/MasterclassLayout.astro returns 5 or more
    - grep -c "highlight-path" src/layouts/MasterclassLayout.astro returns 3 or more
    - grep -c "hero-text-fade" src/layouts/MasterclassLayout.astro returns 2 or more
    - grep -c "hero-decor-slide" src/layouts/MasterclassLayout.astro returns 2 or more
    - grep -c "hero-zigzag-path" src/layouts/MasterclassLayout.astro returns 2 or more
    - grep -c "hero-cta-pulse" src/layouts/MasterclassLayout.astro returns 2 or more
    - grep "'wreath'" src/components/PricingCta.astro returns a match
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>MasterclassLayout has a single shared style block with ALL shared animation CSS including hero animations. PricingCta accepts 'wreath'. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract MasterclassHero component from florarium page</name>
  <files>src/components/MasterclassHero.astro</files>
  <read_first>
    - src/pages/stvorennya-florariumu.astro (lines 101-233 — the ENTIRE hero section, this is the source of truth)
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 100-192 — kartini hero for prop comparison only)
  </read_first>
  <action>
Create `src/components/MasterclassHero.astro` extracted from the florarium hero (lines 101-233). This is the SINGLE SOURCE OF TRUTH layout.

**Props interface:**

```astro
---
interface DecorElement {
  src: string;
  width: number;
  height: number;
  visibility: 'mobile-only' | 'tablet-down' | 'desktop-only' | 'all';
  style: string;
}

interface Props {
  heroId: string;
  bgMobile: string;
  bgDesktop?: string;
  dariaImg: string;
  dariaAlt: string;
  dariaWidth: number;
  dariaHeight: number;
  badgeText: string;
  titleLine1: string;
  titleLine2: string;
  subtitle: string;
  ctaText: string;
  ctaHref: string;
  ctaPulse?: boolean;
  decorElements: DecorElement[];
  bleedElement?: {
    src: string;
    width: number;
    height: number;
    style: string;
  };
}

const {
  heroId, bgMobile, bgDesktop, dariaImg, dariaAlt, dariaWidth, dariaHeight,
  badgeText, titleLine1, titleLine2, subtitle, ctaText, ctaHref, ctaPulse = false,
  decorElements, bleedElement
} = Astro.props;
---
```

**HTML template — copy EXACTLY from florarium lines 103-233, replacing hardcoded values with props:**

The structure MUST be:
1. Outer wrapper: `<div class="relative">`
2. `<section id={heroId} class="relative overflow-hidden rounded-b-[25px] lg:mt-[-90px]" style={`background: #f8f8f8 url('${bgMobile}') no-repeat center/cover;`}>`
3. Desktop Daria — absolutely positioned right half, pinned to bottom: `<div class="hidden lg:block absolute bottom-0 pointer-events-none z-0 lg:left-[54%] xl:left-[58%]" style="right: -80px;">` with `<img src={dariaImg} alt="" width={dariaWidth} height={dariaHeight} class="w-auto object-contain object-bottom lg:max-h-[520px] xl:max-h-[620px]" loading="eager" />`
4. Decorative elements — loop over `decorElements` array. For each element, render `<img>` with visibility class based on `visibility` prop:
   - `'mobile-only'` -> `class="md:hidden absolute pointer-events-none z-[5] hero-decor-slide"`
   - `'tablet-down'` -> `class="lg:hidden absolute pointer-events-none z-[5] hero-decor-slide"`
   - `'desktop-only'` -> `class="hidden lg:block absolute pointer-events-none z-[5] hero-decor-slide"`
   - `'all'` -> `class="absolute pointer-events-none z-[5] hero-decor-slide"`
   Each `<img>` uses `src={el.src} alt="" width={el.width} height={el.height} style={el.style}`
5. Content container: `<div class="relative z-10 max-w-[1280px] mx-auto px-6 md:px-8 pt-[60px] md:pt-[40px] lg:pt-[220px] pb-[20px] md:pb-0 lg:pb-[120px] lg:min-h-[720px] md:flex md:items-end lg:items-center md:min-h-0">`
6. Text content div: `<div class="max-w-[600px] md:max-w-none md:w-1/2 lg:w-[540px] mx-auto md:mx-0 text-center md:text-left md:pb-[60px]">`
7. Badge: `<div class="flex justify-center md:justify-start mb-8 hero-text-fade" style="animation-delay: 0s;">` containing `<div class="inline-flex items-center gap-[8px] rounded-full" style="background: #00401E; padding: 7px 16px;">` with star emoji span + `<span class="font-heading text-white font-medium" style="font-size: 14px; line-height: 1;">{badgeText}</span>`
8. Title h1: `<h1 class="font-heading text-black uppercase mb-7 hero-text-fade" style="line-height: 1.08; letter-spacing: -0.36px; animation-delay: 0.1s;">` with `<span class="block text-[38px] md:text-[44px] lg:text-[68px] xl:text-[89px]">{titleLine1}</span>` and `<span class="relative inline-block text-[38px] md:text-[44px] lg:text-[68px] xl:text-[89px]">{titleLine2}` with the zigzag SVG path inside (copy exact SVG from florarium line 167-182 with `class="hero-zigzag-path"`)
9. Subtitle: `<p class="font-heading text-black mb-8 mx-auto md:mx-0 hero-text-fade" style="font-size: 16px; line-height: 24px; max-width: 340px; animation-delay: 0.2s;">{subtitle}</p>` — NOTE: the subtitle is passed as a string prop, but may contain `<br>` tags. Use `<Fragment set:html={subtitle} />` to render HTML in the subtitle.
10. CTA: `<div class="flex justify-center md:justify-start hero-text-fade" style="animation-delay: 0.3s;">` with `<a href={ctaHref} class={`inline-flex items-center justify-center font-heading text-white font-medium hover:scale-[1.02] transition-transform ${ctaPulse ? 'hero-cta-pulse' : ''}`} style="padding: 16px 30px; background: #F3237D; border-radius: 100px; font-size: 18px;">{ctaText}</a>`
11. Mobile Daria: `<div class="lg:hidden mt-6 md:mt-0 md:w-1/2 flex justify-end relative z-[6] hero-daria-slide" style="margin-right: -14px; margin-bottom: -20px;">` with `<img src={dariaImg} alt={dariaAlt} width={dariaWidth} height={dariaHeight} class="w-full max-w-[232px] md:max-w-[380px]" loading="eager" />`
12. Close section
13. If `bleedElement` provided: render `<img src={bleedElement.src} alt="" width={bleedElement.width} height={bleedElement.height} class="absolute pointer-events-none z-[1] hero-decor-slide" style={bleedElement.style} />`
14. Close wrapper div

If `bgDesktop` is provided, add a `<style is:global>` block at the bottom:
```css
@media (min-width: 768px) {
  #{heroId} {
    background-image: url('{bgDesktop}') !important;
  }
}
```

**CRITICAL:** Do NOT add any other `<style>` blocks. All animation CSS (hero-text-fade, hero-decor-slide, hero-daria-slide, hero-zigzag-path, hero-cta-pulse) is in MasterclassLayout.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/MasterclassHero.astro exists
    - grep "interface Props" src/components/MasterclassHero.astro returns a match
    - grep "interface DecorElement" src/components/MasterclassHero.astro returns a match
    - grep "heroId" src/components/MasterclassHero.astro returns a match
    - grep "dariaImg" src/components/MasterclassHero.astro returns a match
    - grep "titleLine1" src/components/MasterclassHero.astro returns a match
    - grep "hero-zigzag-path" src/components/MasterclassHero.astro returns a match
    - grep "hero-text-fade" src/components/MasterclassHero.astro returns matches (class usage, not definition)
    - grep "hero-decor-slide" src/components/MasterclassHero.astro returns matches
    - grep "hero-daria-slide" src/components/MasterclassHero.astro returns a match
    - grep 'loading="eager"' src/components/MasterclassHero.astro returns matches (hero images must be eager)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>MasterclassHero.astro component exists with typed Props, matching florarium hero structure exactly. Accepts props for all variable content (title, images, decor elements, CTA). Uses shared CSS classes from MasterclassLayout. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Extract TargetAudienceSection and EducationalBlock components</name>
  <files>src/components/TargetAudienceSection.astro, src/components/EducationalBlock.astro</files>
  <read_first>
    - src/pages/stvorennya-florariumu.astro (lines 235-306 — target audience section, SOURCE OF TRUTH)
    - src/pages/stvorennya-florariumu.astro (lines 308-446 — educational block, SOURCE OF TRUTH)
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 194-269 — kartini target audience for data comparison)
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 271-367 — kartini educational block for data comparison)
  </read_first>
  <action>
**Component A: TargetAudienceSection.astro**

Create `src/components/TargetAudienceSection.astro` extracted from florarium lines 235-306. The florarium layout is the SOURCE OF TRUTH.

```astro
---
interface Props {
  titleImage: string;
  titleImageAlt: string;
  subtitle: string;
  cards: Array<{
    image: string;
    imageAlt: string;
    title: string;
    description: string;
  }>;
  decorLeftImage?: string;
  decorLeftStyle?: string;
  decorRightImage?: string;
  decorRightStyle?: string;
}
const { titleImage, titleImageAlt, subtitle, cards, decorLeftImage, decorLeftStyle, decorRightImage, decorRightStyle } = Astro.props;
---
```

HTML structure — copy EXACTLY from florarium lines 236-306:
1. Outer: `<section class="relative bg-white px-[10px] py-[40px] md:px-8 md:py-[56px] lg:py-[48px] overflow-hidden">`
2. Optional decor: if `decorRightImage`, render `<img src={decorRightImage} alt="" width="929" height="906" class="hidden lg:block absolute pointer-events-none z-0" style={decorRightStyle || "top: 2%; right: 1%; width: 160px; height: auto;"} />`. If `decorLeftImage`, render with `class="hidden lg:block absolute pointer-events-none z-0 decor-sway"` and `style={decorLeftStyle || "top: 5%; left: -3%; width: 210px; height: auto; opacity: 0.9; filter: blur(1px); --dur: 7s; --del: -2s;"}`
3. Inner: `<div class="max-w-[1280px] mx-auto relative z-10">`
4. Title image: `<div class="flex justify-center mb-[8px] reveal">` with `<img src={titleImage} alt={titleImageAlt} width="1024" height="460" class="w-full max-w-[300px] md:max-w-[400px] lg:max-w-[460px] h-auto" loading="lazy" />`
5. Subtitle: `<p class="text-brand-dark/70 text-[16px] md:text-[18px] leading-[28px] text-center mb-[40px] max-w-[780px] mx-auto reveal">{subtitle}</p>`
6. Cards grid: `<div class="grid grid-cols-1 md:grid-cols-2 gap-[16px] md:gap-[20px]">`
7. Each card: `<div class="bg-white rounded-[20px] p-[20px] flex flex-col items-center text-center lg:flex-row lg:items-start lg:text-left gap-[16px] lg:gap-[20px] reveal">` with image (`flex-shrink-0 w-[200px] h-[200px] md:w-[140px] md:h-[140px] lg:w-[160px] lg:h-[160px] object-contain`, `width="800" height="783"`) + div with h3 (`font-heading text-[#1a1a1a] text-[20px] md:text-[22px] leading-[1.3] mb-[8px]`) + p (`text-[#8a8a8a] text-[15px] leading-[24px] text-left`)

NOTE: Florarium cards have NO border, use `lg:flex-row` breakpoint, image sizes `200/140/160px`. This is the correct layout per the source of truth. Kartini/vinok pages currently have borders and different sizing — those are WRONG and will be corrected when rebuilt.

**CRITICAL: Do NOT add any `<style>` block.**

**Component B: EducationalBlock.astro**

Create `src/components/EducationalBlock.astro` extracted from florarium lines 308-446.

```astro
---
interface HotspotItem {
  label: string;
  left: string;
  top: string;
}

interface Props {
  titleImage: string;
  titleImageAlt: string;
  headingHighlight: string;
  headingRest: string;
  productImage: string;
  productImageAlt: string;
  productImageWidth: number;
  productImageHeight: number;
  hotspots: HotspotItem[];
  benefits: string[];
  quoteImage: string;
  quoteImageAlt: string;
  memojiImage?: string;
  memojiStyle?: string;
}

const {
  titleImage, titleImageAlt, headingHighlight, headingRest,
  productImage, productImageAlt, productImageWidth, productImageHeight,
  hotspots, benefits, quoteImage, quoteImageAlt, memojiImage, memojiStyle
} = Astro.props;
---
```

HTML structure — copy from florarium lines 309-446:
1. Outer: `<section class="relative bg-white px-[10px] py-[24px] md:px-8 md:py-[56px] lg:py-[48px] lg:pb-[48px] overflow-hidden">`
2. Optional memoji: if `memojiImage`, render `<img src={memojiImage} alt="" width="180" height="200" class="hidden lg:block absolute pointer-events-none z-0" style={memojiStyle || "top: 15%; right: 2%; width: 130px; height: auto; rotate: 15deg;"} />`
3. Inner: `<div class="max-w-[1280px] mx-auto relative z-10">`
4. Title image centered with reveal class
5. `<div class="lg:grid lg:grid-cols-2 lg:gap-[48px] lg:items-center mt-[8px] lg:mt-[24px]">`
6. Left column: mobile heading with highlight-path SVG (visible below lg, hidden on lg), then product image with hotspot buttons inside `florarium-hotspot-container`
7. Right column: desktop heading (hidden below lg), benefits list with green checkmark circles, quote image
8. The heading text structure: `<span class="relative inline"><span class="relative z-10 font-heading">{headingHighlight} </span>` + highlight-path SVG + `</span>{headingRest}` — The heading uses the SAME highlight-path SVG from florarium (path d="M14.4,111.6c0,0,202.9-33.7,471.2,0c0,0-194-8.9-397.3,24.7c0,0,141.9-5.9,309.2,0" stroke="#a5d100" stroke-width="38")
9. The heading appears TWICE: once for mobile/tablet (`lg:hidden`, text centered, `text-[22px] md:text-[28px]`) and once for desktop (`hidden lg:block`, text left, `lg:text-[42px] xl:text-[44px]`)
10. Hotspot buttons: loop over `hotspots` array, render each as `<button class="hotspot-btn" style={`left: ${h.left}; top: ${h.top};`} aria-label={h.label}>` with + SVG icon and tooltip span
11. Benefits: loop with green checkmark circles (28px, bg #7fac00)
12. Quote image at bottom of right column

Include the hotspot toggle `<script>` at the bottom of the component:
```html
<script>
  document.querySelectorAll('.hotspot-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.stopPropagation();
      const wasActive = btn.classList.contains('active');
      document.querySelectorAll('.hotspot-btn.active').forEach(b => b.classList.remove('active'));
      if (!wasActive) btn.classList.add('active');
    });
  });
  document.addEventListener('click', () => {
    document.querySelectorAll('.hotspot-btn.active').forEach(b => b.classList.remove('active'));
  });
</script>
```

**CRITICAL: Do NOT add any `<style>` block to this component.** All CSS (hotspot-btn, highlight-path, florarium-hotspot-container) is in MasterclassLayout.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/TargetAudienceSection.astro exists
    - File src/components/EducationalBlock.astro exists
    - grep "interface Props" src/components/TargetAudienceSection.astro returns a match
    - grep "interface Props" src/components/EducationalBlock.astro returns a match
    - grep "interface HotspotItem" src/components/EducationalBlock.astro returns a match
    - grep "hotspot-btn" src/components/EducationalBlock.astro returns matches
    - grep "florarium-hotspot-container" src/components/EducationalBlock.astro returns a match
    - grep "highlight-path" src/components/EducationalBlock.astro returns matches
    - grep "headingHighlight" src/components/EducationalBlock.astro returns a match
    - grep -c '<style' src/components/TargetAudienceSection.astro returns 0
    - grep -c '<style' src/components/EducationalBlock.astro returns 0
    - grep 'width="800" height="783"' src/components/TargetAudienceSection.astro returns a match
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>TargetAudienceSection and EducationalBlock components exist with typed Props, matching florarium HTML exactly. Both use shared CSS classes. Zero style blocks. Build passes.</done>
</task>

</tasks>

<verification>
- `cd D:/projects/Daria/wife-website && npm run build` passes with zero errors
- MasterclassLayout.astro has `<style is:global>` with ALL shared CSS (decor animations, hero animations, hotspot, highlight-path, slide-in, bulb, gallery, curriculum-img, reduced motion)
- PricingCta course union includes 'wreath'
- 3 new component files exist: MasterclassHero.astro, TargetAudienceSection.astro, EducationalBlock.astro
- All 3 new components have typed Props interfaces
- Zero `<style>` blocks in any new component (except MasterclassHero which may have a conditional bgDesktop `<style is:global>`)
- No existing page is broken — components are new files, not yet imported
</verification>

<success_criteria>
- Shared CSS centralized in MasterclassLayout (single source)
- Hero animations (hero-text-fade, hero-decor-slide, hero-daria-slide, hero-zigzag-path, hero-cta-pulse) in shared CSS
- 3 new components created from florarium source of truth
- PricingCta TypeScript type fixed
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-01-SUMMARY.md`
</output>
