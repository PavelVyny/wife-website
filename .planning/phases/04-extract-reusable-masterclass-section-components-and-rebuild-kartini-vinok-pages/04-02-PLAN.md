---
phase: 04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WhatsIncludedSection.astro
  - src/components/LearningOutcomes.astro
  - src/components/CurriculumSection.astro
  - src/components/FaqSection.astro
  - src/components/CtaButton.astro
autonomous: true
requirements: [P4-COMPONENTS]

must_haves:
  truths:
    - "WhatsIncludedSection renders lightbulb SVG, title image, checklist with optional bonus item, and overlapping product images"
    - "LearningOutcomes renders title image, bullet list, two overlapping images with optional decor overlay"
    - "CurriculumSection renders title image and lessons with zigzag or stacked layout, numbered circles with highlight-path SVG underlines"
    - "FaqSection renders title image, optional decor elements, and FaqAccordion component"
    - "CtaButton renders a centered pink CTA button linking to #pricing"
    - "npm run build passes with zero errors"
  artifacts:
    - path: "src/components/WhatsIncludedSection.astro"
      provides: "Whats included checklist + overlapping images"
      contains: "interface Props"
    - path: "src/components/LearningOutcomes.astro"
      provides: "Learning outcomes list + images"
      contains: "interface Props"
    - path: "src/components/CurriculumSection.astro"
      provides: "Curriculum timeline with zigzag/stacked layouts"
      contains: "interface Props"
    - path: "src/components/FaqSection.astro"
      provides: "FAQ section wrapper with FaqAccordion"
      contains: "FaqAccordion"
    - path: "src/components/CtaButton.astro"
      provides: "Simple CTA button"
      contains: "interface Props"
  key_links:
    - from: "src/components/FaqSection.astro"
      to: "src/components/FaqAccordion.astro"
      via: "import + component usage"
      pattern: "import FaqAccordion"
---

<objective>
Extract the remaining 5 section components from the florarium reference page: WhatsIncludedSection, LearningOutcomes, CurriculumSection, FaqSection, and CtaButton.

Purpose: Complete the full component library so all 3 masterclass pages can be rebuilt as thin wrappers of data + components.
Output: 5 new Astro components with typed Props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-RESEARCH.md

@src/pages/stvorennya-florariumu.astro
@src/components/FaqAccordion.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract WhatsIncludedSection, LearningOutcomes, and CurriculumSection</name>
  <files>src/components/WhatsIncludedSection.astro, src/components/LearningOutcomes.astro, src/components/CurriculumSection.astro</files>
  <read_first>
    - src/pages/stvorennya-florariumu.astro (lines 448-550 — WhatsIncluded section, SOURCE OF TRUTH)
    - src/pages/stvorennya-florariumu.astro (lines 561-637 — LearningOutcomes section, SOURCE OF TRUTH)
    - src/pages/stvorennya-florariumu.astro (lines 645-765 — Curriculum section, SOURCE OF TRUTH)
    - src/pages/stvorennya-florariumu.astro (lines 856-880 — curriculum-timeline CSS for florarium zigzag)
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (lines 539-632 — kartini curriculum for stacked layout comparison)
    - src/pages/vinok-zi-stabilizovanih-roslin.astro (lines 538-657 — vinok curriculum for bullets comparison)
  </read_first>
  <action>
**Component A: WhatsIncludedSection.astro**

Extract from florarium lines 448-550. Create `src/components/WhatsIncludedSection.astro`:

```astro
---
interface Props {
  titleImage: string;
  titleImageAlt: string;
  items: string[];
  bonusItem?: string;
  productImg1: string;
  productImg1Alt: string;
  productImg2: string;
  productImg2Alt: string;
  decorMossImage?: string;
  decorMossStyle?: string;
  bleedCookieImage?: string;
  bleedCookieStyle?: string;
}
const { titleImage, titleImageAlt, items, bonusItem, productImg1, productImg1Alt, productImg2, productImg2Alt, decorMossImage, decorMossStyle, bleedCookieImage, bleedCookieStyle } = Astro.props;
---
```

HTML structure — copy from florarium lines 449-550:
1. Outer wrapper: `<div class="relative">` (needed for bleed cookie that overflows section)
2. `<section class="relative bg-white px-[10px] md:px-8 py-[24px] md:py-[56px] lg:py-[48px] pb-[80px] overflow-hidden">`
3. Optional decor moss: if `decorMossImage`, render `<img src={decorMossImage} alt="" width="200" height="200" class="hidden lg:block absolute pointer-events-none z-0 decor-sway" style={decorMossStyle || "top: 5%; left: -2%; width: 90px; height: auto; opacity: 0.7; --dur: 6s; --del: -2s;"} />`
4. `<div class="max-w-[1280px] mx-auto">`
5. Lightbulb + title: `<div class="flex justify-center mb-[16px] reveal"><div class="relative">` — copy the EXACT lightbulb SVG from florarium lines 459-474 (bulb-glow class, 5 bulb-ray lines, bulb body path, shine, base, tip). Then `<img src={titleImage} alt={titleImageAlt} width="1024" height="460" class="w-full max-w-[320px] md:max-w-[420px] lg:max-w-[460px] h-auto" loading="lazy" />`
6. `<div class="lg:grid lg:grid-cols-2 lg:gap-[48px] lg:items-center mt-[24px]">`
7. Left column (reveal div): checklist `<ul class="space-y-[20px] px-[14px] md:px-0">` — map over `items` array, each `<li class="text-[#333] text-[18px] leading-[26px] flex gap-[12px] items-center font-heading font-medium">` with green checkmark circle (28px, bg #7fac00). If `bonusItem` provided, render as last `<li>` with pink circle (bg #F3237D) and after the ul, add the curved arrow doodle + "Бонус" badge (copy exact SVG from florarium lines 512-519).
8. Right column: overlapping product images in `<div class="mt-[24px] lg:mt-0 relative" style="min-height: 380px;">` with `productImg1` as `class="rounded-[30px] reveal included-slide-left" style="width: 86%; display: block;"` (width="1024" height="1024") and `productImg2` as `class="rounded-[30px] absolute reveal included-slide-right" style="width: 50%; bottom: -20px; right: 0; box-shadow: 0 8px 30px rgba(0,0,0,0.1);"` (width="1024" height="1024")
9. Close section
10. Optional bleed cookie: if `bleedCookieImage`, render `<img src={bleedCookieImage} alt="" width="280" height="268" class="absolute pointer-events-none z-10" style={bleedCookieStyle} />`
11. Close wrapper div

**CRITICAL: Do NOT add any `<style>` block.** All CSS (bulb-glow, bulb-ray, included-slide-left/right, decor-sway) is in MasterclassLayout.

**Component B: LearningOutcomes.astro**

Extract from florarium lines 561-637. Create `src/components/LearningOutcomes.astro`:

```astro
---
interface Props {
  titleImage: string;
  titleImageAlt?: string;
  items: string[];
  img1: string;
  img1Alt: string;
  img2: string;
  img2Alt: string;
  decorImg?: string;
  decorStyle?: string;
  topDecorImage?: string;
  topDecorStyle?: string;
}
const { titleImage, titleImageAlt = 'Результати які ви отримаєте', items, img1, img1Alt, img2, img2Alt, decorImg, decorStyle, topDecorImage, topDecorStyle } = Astro.props;
---
```

HTML structure — copy from florarium lines 562-637:
1. `<section class="relative px-[10px] md:px-8 py-[40px] md:py-[56px] lg:py-[48px] pb-[80px] overflow-hidden" style="background: #f8f8f8;">`
2. Optional top decor: if `topDecorImage`, render `<img src={topDecorImage} alt="" width="80" height="80" class="hidden lg:block absolute pointer-events-none z-10" style={topDecorStyle} loading="lazy" />`
3. `<div class="max-w-[1280px] mx-auto">`
4. Title — mobile/tablet only: `<div class="flex justify-center mb-[16px] lg:hidden reveal">` with `<img src={titleImage} alt={titleImageAlt} width="1024" height="460" class="w-full max-w-[320px] md:max-w-[420px] h-auto" loading="lazy" />`
5. `<div class="lg:grid lg:grid-cols-2 lg:gap-[48px] lg:items-center mt-[24px]">`
6. Left — images: `<div class="mt-[24px] lg:mt-0 relative lg:order-1" style="min-height: 380px;">` with `img1` as `included-slide-left` (width="1024" height="1024", 86% width) and `img2` as `included-slide-right` (50% width, bottom: -20px). If `decorImg`, render `<img src={decorImg} alt="" width="200" height="200" class="absolute pointer-events-none reveal" style={decorStyle} loading="lazy" />`
7. Right — title (desktop) + bullets: `<div class="mt-[40px] lg:mt-0 lg:order-2 reveal">`. Desktop title: `<div class="hidden lg:flex justify-center lg:justify-start mb-[24px]">` with same title image at `max-w-[460px]`. Then `<ul class="space-y-[20px] px-[14px] md:px-0">` with green checkmark items (same pattern as WhatsIncluded, text color `#333`, `text-[18px] leading-[28px] font-heading`)

**CRITICAL: Do NOT add any `<style>` block.**

**Component C: CurriculumSection.astro**

Extract from florarium lines 645-765. Create `src/components/CurriculumSection.astro`:

```astro
---
interface LessonItem {
  title: string;
  titleRest: string;
  description?: string;
  bullets?: string[];
  imageMobile: string;
  imageDesktop: string;
  imageAlt: string;
}
interface Props {
  titleImage: string;
  titleImageAlt: string;
  lessons: LessonItem[];
  layout?: 'zigzag' | 'stacked';
  timelineClass?: string;
  decorImage?: string;
  decorStyle?: string;
}
const { titleImage, titleImageAlt, lessons, layout = 'zigzag', timelineClass = 'curriculum-timeline', decorImage, decorStyle } = Astro.props;
---
```

HTML structure — extracted from florarium lines 646-764:
1. `<section class="relative bg-white px-[10px] py-[40px] md:px-8 md:py-[56px] lg:py-[48px] overflow-hidden">`
2. Optional decor: if `decorImage`, render `<img src={decorImage} alt="" width="190" height="210" class="hidden lg:block absolute pointer-events-none z-0 decor-anim" style={decorStyle} />`
3. `<div class="max-w-[1280px] mx-auto">`
4. Title image centered with reveal
5. Timeline container: `<div class={timelineClass + ' relative'}>`
6. For each lesson (index `i`, starting from 0):
   - Determine if this lesson's image is on the left or right. For `layout='zigzag'`: even index (0, 2) = image left/text right (`lg:flex`), odd index (1, 3) = text left/image right (`lg:flex lg:flex-row-reverse`). For `layout='stacked'`: always image left (`lg:flex lg:items-start lg:gap-[48px]`).
   - Each lesson block: `<div class="relative mb-[48px] md:mb-[64px] reveal px-[4px] md:px-0 {layoutClasses}">`
   - Desktop image: `<div class="hidden lg:flex {zigzag ? 'lg:w-[46%]' : 'lg:w-[40%]'} justify-center flex-shrink-0">` with `<img src={lesson.imageDesktop} alt={lesson.imageAlt} width="1024" height="1024" class="w-[380px] rounded-[20px]" loading="lazy" />`
   - Text div: `<div class="{zigzag ? 'lg:w-[46%]' : 'lg:w-[60%]'} {zigzag && isEven ? 'lg:pl-[40px]' : ''} {zigzag && isOdd ? 'lg:text-right lg:pr-[40px]' : ''}">`
   - Number + title row: `<div class="flex items-start gap-[12px] mb-[12px] {zigzag && isOdd ? 'lg:justify-end' : ''}">` with number circle `<span class="flex-shrink-0 w-[36px] h-[36px] lg:w-[46px] lg:h-[46px] rounded-full flex items-center justify-center font-heading text-white text-[16px] lg:text-[20px] font-bold {zigzag ? 'lg:absolute lg:left-1/2 lg:-translate-x-1/2 lg:top-0 lg:z-10' : ''}" style="background: #7fac00;">{i + 1}</span>` and `<h3 class="font-heading text-black text-[26px] leading-[1.2]">` with highlight-path SVG on `lesson.title`, then `<br/>{lesson.titleRest}`
   - The highlight-path SVG: `<span class="relative inline"><span class="relative z-10">{lesson.title}</span><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 500 150" class="absolute -bottom-[4px] left-0 w-full h-[14px] overflow-visible pointer-events-none" preserveAspectRatio="none"><path class="highlight-path" d="M14.4,111.6c0,0,202.9-33.7,471.2,0c0,0-194-8.9-397.3,24.7c0,0,141.9-5.9,309.2,0" fill="none" stroke="#a5d100" stroke-width="38" stroke-linecap="round"/></svg></span>`
   - If `lesson.description`: render `<p class="text-[#7b7b7b] text-[18px] leading-[28px] font-heading ml-[48px] {zigzag ? 'lg:ml-0' : ''}">{lesson.description}</p>`
   - If `lesson.bullets`: render `<ul class="space-y-[16px] ml-[48px] mt-[8px]">` with each bullet as `<li class="text-[#7b7b7b] text-[18px] leading-[28px] flex gap-[12px] items-center font-heading">` with small green checkmark (24px circle, bg #7fac00)
   - Mobile image below: `<div class="flex justify-center mb-[48px] reveal lg:hidden">` with `<img src={lesson.imageMobile} alt={lesson.imageAlt} width="1024" height="1024" class="w-[280px] h-auto" loading="lazy" />` (last lesson: `w-[340px]` and no `mb-[48px]`)

**NOTE about timeline CSS:** The page-specific CSS for `.curriculum-timeline` (florarium) and `.curriculum-timeline-k` (kartini/vinok) stays on each page in their own `<style is:global>` since the timeline positioning differs. The component applies the class name via the `timelineClass` prop.

**CRITICAL: Do NOT add any `<style>` block.** All CSS (highlight-path, curriculum-img) is in MasterclassLayout. Timeline classes stay page-specific.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/WhatsIncludedSection.astro exists
    - File src/components/LearningOutcomes.astro exists
    - File src/components/CurriculumSection.astro exists
    - grep "interface Props" src/components/WhatsIncludedSection.astro returns a match
    - grep "interface Props" src/components/LearningOutcomes.astro returns a match
    - grep "interface Props" src/components/CurriculumSection.astro returns a match
    - grep "bonusItem" src/components/WhatsIncludedSection.astro returns a match
    - grep "bulb-glow" src/components/WhatsIncludedSection.astro returns a match
    - grep "included-slide-left" src/components/WhatsIncludedSection.astro returns a match
    - grep "decorImg" src/components/LearningOutcomes.astro returns a match
    - grep "zigzag" src/components/CurriculumSection.astro returns a match
    - grep "highlight-path" src/components/CurriculumSection.astro returns matches
    - grep "timelineClass" src/components/CurriculumSection.astro returns a match
    - grep "bullets" src/components/CurriculumSection.astro returns a match
    - grep -c '<style' src/components/WhatsIncludedSection.astro returns 0
    - grep -c '<style' src/components/LearningOutcomes.astro returns 0
    - grep -c '<style' src/components/CurriculumSection.astro returns 0
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>WhatsIncludedSection, LearningOutcomes, and CurriculumSection components exist with typed Props, matching florarium HTML exactly. Zero style blocks. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract FaqSection and CtaButton components</name>
  <files>src/components/FaqSection.astro, src/components/CtaButton.astro</files>
  <read_first>
    - src/pages/stvorennya-florariumu.astro (lines 767-799 — CTA + FAQ section, SOURCE OF TRUTH)
    - src/components/FaqAccordion.astro (first 10 lines — Props interface)
  </read_first>
  <action>
**Component A: FaqSection.astro**

Extract from florarium lines 779-799. Create `src/components/FaqSection.astro`:

```astro
---
import FaqAccordion from './FaqAccordion.astro';

interface Props {
  titleImage: string;
  titleImageAlt?: string;
  faqs: Array<{ q: string; a: string[] }>;
  decorTopRight?: { src: string; style: string };
  decorBottomLeft?: { src: string; style: string };
}
const { titleImage, titleImageAlt = 'Питання та відповідь', faqs, decorTopRight, decorBottomLeft } = Astro.props;
---
```

HTML structure — from florarium lines 779-799:
1. `<section id="faq" class="relative bg-white px-[10px] py-[40px] md:px-8 md:py-[56px] lg:py-[48px] overflow-hidden">`
2. Optional decorTopRight: if provided, `<img src={decorTopRight.src} alt="" width="200" height="200" class="hidden lg:block absolute pointer-events-none z-0" style={decorTopRight.style} />`
3. Optional decorBottomLeft: if provided, `<img src={decorBottomLeft.src} alt="" width="160" height="180" class="hidden lg:block absolute pointer-events-none z-0 decor-anim" style={decorBottomLeft.style} />`
4. `<div class="max-w-[1280px] mx-auto">`
5. Title image: `<div class="flex justify-center mb-[20px] reveal">` with `<img src={titleImage} alt={titleImageAlt} width="1024" height="460" class="w-full max-w-[280px] md:max-w-[380px] lg:max-w-[420px] h-auto" loading="lazy" />`
6. `<FaqAccordion faqs={faqs} />`

**CRITICAL: Do NOT add any `<style>` block.**

**Component B: CtaButton.astro**

Extract from florarium lines 768-777. Create `src/components/CtaButton.astro`:

```astro
---
interface Props {
  text?: string;
  href?: string;
}
const { text = 'Придбати зараз', href = '#pricing' } = Astro.props;
---
```

HTML:
```html
<div class="flex justify-center py-[32px] bg-white reveal">
  <a
    href={href}
    class="inline-flex items-center justify-center font-heading text-white font-medium hover:scale-[1.02] transition-transform"
    style="padding: 16px 40px; background: #E63380; border-radius: 100px; font-size: 20px;"
  >
    {text}
  </a>
</div>
```

No style block needed — just a simple wrapper.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/FaqSection.astro exists
    - File src/components/CtaButton.astro exists
    - grep "import FaqAccordion" src/components/FaqSection.astro returns a match
    - grep "interface Props" src/components/FaqSection.astro returns a match
    - grep "interface Props" src/components/CtaButton.astro returns a match
    - grep "decorTopRight" src/components/FaqSection.astro returns a match
    - grep "#pricing" src/components/CtaButton.astro returns a match
    - grep -c '<style' src/components/FaqSection.astro returns 0
    - grep -c '<style' src/components/CtaButton.astro returns 0
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>FaqSection and CtaButton components exist with typed Props. FaqSection imports and uses FaqAccordion. Zero style blocks. Build passes.</done>
</task>

</tasks>

<verification>
- `cd D:/projects/Daria/wife-website && npm run build` passes with zero errors
- 5 new component files exist in src/components/
- FaqSection imports FaqAccordion (not duplicating accordion logic)
- CurriculumSection supports both 'zigzag' and 'stacked' layouts
- WhatsIncludedSection supports optional bonusItem with pink badge
- All 5 components have zero `<style>` blocks
</verification>

<success_criteria>
- 5 new Astro components created from florarium source of truth
- Each has typed Props interface
- None contain style blocks
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-02-SUMMARY.md`
</output>
