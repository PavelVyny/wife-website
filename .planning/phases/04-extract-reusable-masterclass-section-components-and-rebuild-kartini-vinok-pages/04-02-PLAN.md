---
phase: 04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/pages/kartini-zi-stabilizovanih-roslin.astro
autonomous: true
requirements: [P4-KARTINI, P4-DEDUP]

must_haves:
  truths:
    - "kartini page uses TargetAudienceSection, WhatsIncludedSection, LearningOutcomes, and CurriculumSection components instead of inline HTML"
    - "kartini page's <style is:global> block no longer contains shared animation classes (decor-anim, decor-sway, bulb-glow, included-slide-left, hotspot-btn, highlight-path, gallery-img)"
    - "kartini page renders identically before and after refactor"
    - "npm run build passes with zero errors"
  artifacts:
    - path: "src/pages/kartini-zi-stabilizovanih-roslin.astro"
      provides: "Refactored kartini page using shared components"
      contains: "TargetAudienceSection"
  key_links:
    - from: "src/pages/kartini-zi-stabilizovanih-roslin.astro"
      to: "src/components/TargetAudienceSection.astro"
      via: "import + component usage"
      pattern: "import TargetAudienceSection"
    - from: "src/pages/kartini-zi-stabilizovanih-roslin.astro"
      to: "src/components/WhatsIncludedSection.astro"
      via: "import + component usage"
      pattern: "import WhatsIncludedSection"
    - from: "src/pages/kartini-zi-stabilizovanih-roslin.astro"
      to: "src/components/CurriculumSection.astro"
      via: "import + component usage"
      pattern: "import CurriculumSection"
    - from: "src/pages/kartini-zi-stabilizovanih-roslin.astro"
      to: "src/components/LearningOutcomes.astro"
      via: "import + component usage"
      pattern: "import LearningOutcomes"
---

<objective>
Rebuild the kartini page to use the extracted shared components, removing duplicated inline HTML and CSS.

Purpose: Replace 400+ lines of inline section HTML with component calls, remove shared CSS that now lives in MasterclassLayout.
Output: Slimmer kartini page using shared components, identical visual output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-RESEARCH.md
@.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-01-SUMMARY.md

@src/pages/kartini-zi-stabilizovanih-roslin.astro
@src/components/TargetAudienceSection.astro
@src/components/WhatsIncludedSection.astro
@src/components/LearningOutcomes.astro
@src/components/CurriculumSection.astro

<interfaces>
From src/components/TargetAudienceSection.astro:
```typescript
interface Props {
  titleImage: string;
  titleImageAlt: string;
  subtitle: string;
  cards: Array<{
    image: string;
    imageAlt: string;
    title: string;
    description: string;
  }>;
  decorLeftImage?: string;
  decorRightImage?: string;
}
```

From src/components/WhatsIncludedSection.astro:
```typescript
interface Props {
  titleImage: string;
  titleImageAlt: string;
  items: string[];
  bonusItem?: string;
  productImg1: string;
  productImg1Alt: string;
  productImg2: string;
  productImg2Alt: string;
}
```

From src/components/LearningOutcomes.astro:
```typescript
interface Props {
  titleImage: string;
  titleImageAlt?: string;
  items: string[];
  img1: string;
  img1Alt: string;
  img2: string;
  img2Alt: string;
  decorImg?: string;
  decorStyle?: string;
}
```

From src/components/CurriculumSection.astro:
```typescript
interface LessonItem {
  title: string;
  titleRest: string;
  description?: string;
  bullets?: string[];
  imageMobile: string;
  imageDesktop: string;
  imageAlt: string;
}
interface Props {
  titleImage: string;
  titleImageAlt: string;
  lessons: LessonItem[];
  layout?: 'zigzag' | 'stacked';
  timelineClass?: string;
  decorImage?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace kartini inline sections with component imports</name>
  <files>src/pages/kartini-zi-stabilizovanih-roslin.astro</files>
  <read_first>
    - src/pages/kartini-zi-stabilizovanih-roslin.astro (full file — 931 lines)
    - src/components/TargetAudienceSection.astro (to see exact prop names)
    - src/components/WhatsIncludedSection.astro (to see exact prop names)
    - src/components/LearningOutcomes.astro (to see exact prop names)
    - src/components/CurriculumSection.astro (to see exact prop names and LessonItem shape)
  </read_first>
  <action>
**Step A — Add imports in the frontmatter (after existing imports, before `const PURCHASE_URL`):**

```javascript
import TargetAudienceSection from '../components/TargetAudienceSection.astro';
import WhatsIncludedSection from '../components/WhatsIncludedSection.astro';
import LearningOutcomes from '../components/LearningOutcomes.astro';
import CurriculumSection from '../components/CurriculumSection.astro';
```

**Step B — Add data arrays in the frontmatter (before the closing `---`):**

```javascript
const audienceCards = [
  {
    image: "/images/masterclass-kartini/audience-beginners.webp",
    imageAlt: "Новачки та початківці",
    title: "Новачки та початківці",
    description: "Ідеально підійде тим, хто ніколи не працював зі стабілізованими рослинами. Ви отримаєте базові знання про матеріали, принципи побудови композиції, правила поєднання кольорів, підходящий алгоритм створення картини",
  },
  {
    image: "/images/masterclass-kartini/audience-decor.webp",
    imageAlt: "Шанувальники декору",
    title: "Шанувальники декору",
    description: "Ідеальний формат для тих, хто любить стильні деталі в інтер'єрі. Картина зі стабілізованих рослин не потребує поливу та догляду, але при цьому має вигляд живого, дорогого та прикрашає кожен простір",
  },
  {
    image: "/images/masterclass-kartini/audience-inspiration.webp",
    imageAlt: "Ті, хто шукає натхнення",
    title: "Ті, хто шукає натхнення",
    description: "Майстер-клас стане простором для творчого перезавантаження. Робота з природними текстурами чудово знімає стрес, допомагає відволіктись та дає задоволення від створеного результату",
  },
  {
    image: "/images/masterclass-kartini/audience-gifts.webp",
    imageAlt: "Любителі подарунків",
    title: "Любителі подарунків",
    description: "Картина, створена власноруч, — це особливий подарунок. Ви зможете зробити унікальну річ для близьких, створити декор на замовлення або подарувати собі новий творчий досвід",
  },
];

const includedItems = [
  "Три покрокові відео-уроки",
  "Повний список матеріалів із зазначенням, де їх купити (з посиланнями на магазини)",
  "Рекомендації щодо вибору рослин та інструментів",
  "Технологічна карта круглого панно",
  "Рекомендації з догляду за стабілізованими рослинами",
  "Приклади робіт для натхнення",
];

const learningItems = [
  // Extract the existing learning outcomes bullet items from the kartini page's results section
  // Read the actual content from the current file
];

const curriculumLessons = [
  // Extract lesson data from the kartini page's curriculum section
  // Each lesson needs: title, titleRest, description, imageMobile, imageDesktop, imageAlt
  // Read the actual content from the current file
];
```

NOTE: The `learningItems` and `curriculumLessons` arrays must be populated with the EXACT content currently in the kartini page's inline sections. Read the file first and extract verbatim.

**Step C — Replace inline HTML sections with component calls:**

1. Replace the `{/* ===== TARGET AUDIENCE ===== */}` section (lines ~194-269) with:
```astro
<TargetAudienceSection
  titleImage="/images/masterclass/for-who-title.webp"
  titleImageAlt="Для кого цей майстер-клас"
  subtitle="Цей майстер-клас для тих, хто хоче створити стильний природний декор своїми руками, додати затишку в інтер'єр і спробувати новий формат творчості без складнощів і спеціальної підготовки"
  cards={audienceCards}
  decorLeftImage="/images/masterclass/decor-herb-cluster.webp"
  decorRightImage="/images/masterclass/decor-right-plant.webp"
/>
```

2. Replace the `{/* ===== WHAT'S INCLUDED ===== */}` section (lines ~369-440) with:
```astro
<WhatsIncludedSection
  titleImage="/images/masterclass/whats-included-title.webp"
  titleImageAlt="Що входить у майстер-клас"
  items={includedItems}
  productImg1="/images/masterclass-kartini/included-img-1.webp"
  productImg1Alt="Картина зі стабілізованих рослин"
  productImg2="/images/masterclass-kartini/included-img-2.webp"
  productImg2Alt="Деталі картини зі стабілізованих рослин"
/>
```

3. Replace the Learning Outcomes section with `<LearningOutcomes ... />` passing the data from the current inline section.

4. Replace the Curriculum section with `<CurriculumSection ... />` passing `layout="stacked"` and `timelineClass="curriculum-timeline-k"`.

**Step D — Clean up the `<style is:global>` block:**

Remove from the kartini page's `<style is:global>` ALL classes that are now in MasterclassLayout:
- Remove: `@keyframes decorFloat`, `@keyframes decorSway`, `@keyframes decorFloatMirror`, `.decor-anim`, `.decor-sway`, `.decor-anim-mirror`
- Remove: `.curriculum-img`, `.curriculum-img:hover`
- Remove: `.gallery-img`, `.gallery-item:hover .gallery-img`, `.gallery-overlay`
- Remove: `.bulb-glow`, `.bulb-ray`, `@keyframes bulbRayPulse`
- Remove: `.included-slide-left`, `.included-slide-right`, `.included-slide-left.revealed`, `.included-slide-right.revealed`
- Remove: `.florarium-hotspot-container`, `.hotspot-btn` and all hotspot-related CSS, `@keyframes hotspot-ripple`, `.hotspot-tooltip` rules
- Remove: `.highlight-path`, `.revealed .highlight-path`
- Remove the first `@media (prefers-reduced-motion: reduce)` block (lines ~714-726) since those are shared classes

KEEP on the page (page-specific CSS):
- `.stabilized-bob` + `@keyframes stabilizedBob` (page-specific float animation)
- `.hero-decor-slide-k` + `@keyframes heroDecorSlideInK` (page-specific hero animation)
- `.hero-daria-slide-k` (page-specific)
- `.hero-text-fade-k` + `@keyframes heroTextFadeK` (page-specific)
- `.kartini-cta-btn` + `@keyframes kartiniPinkPulse` (page-specific CTA pulse)
- `.curriculum-timeline-k` + responsive variants (page-specific timeline positioning)
- The second `@media (prefers-reduced-motion: reduce)` block for page-specific animations (hotspot-btn rules can be removed since hotspot CSS is now in MasterclassLayout, but keep the `.stabilized-bob` one)

The `<script>` block for hotspot tooltips MUST remain on the page since the EducationalBlock section is still inline and uses hotspots.

**IMPORTANT**: Do NOT modify the Hero section, EducationalBlock ("Що таке стабілізовані рослини") section, PricingCta usage, InstructorSection usage, ValueProps usage, or FAQ section. These stay as-is.
  </action>
  <verify>
    <automated>cd D:/projects/Daria/wife-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "import TargetAudienceSection" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match
    - grep "import WhatsIncludedSection" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match
    - grep "import LearningOutcomes" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match
    - grep "import CurriculumSection" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match
    - grep "<TargetAudienceSection" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match
    - grep "<WhatsIncludedSection" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match
    - grep "<LearningOutcomes" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match
    - grep "<CurriculumSection" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match
    - grep -c "decor-anim" src/pages/kartini-zi-stabilizovanih-roslin.astro returns 0 (removed from style block; class usage is now in components)
    - grep -c "bulb-glow" src/pages/kartini-zi-stabilizovanih-roslin.astro returns 0 (moved to component)
    - grep "curriculum-timeline-k" src/pages/kartini-zi-stabilizovanih-roslin.astro returns a match (page-specific CSS kept)
    - grep "hero-decor-slide-k" src/pages/kartini-zi-stabilizovanih-roslin.astro returns matches (page-specific kept)
    - wc -l src/pages/kartini-zi-stabilizovanih-roslin.astro shows significantly fewer lines than 931
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>kartini page uses all 4 shared components, shared CSS removed from page's style block, page-specific CSS preserved. Build passes. Page should render identically.</done>
</task>

</tasks>

<verification>
- `cd D:/projects/Daria/wife-website && npm run build` passes
- kartini page imports and uses TargetAudienceSection, WhatsIncludedSection, LearningOutcomes, CurriculumSection
- kartini page `<style is:global>` only contains page-specific CSS (hero animations, CTA pulse, timeline positioning, stabilized-bob)
- No visual regression on kartini page (verify via dev server if possible)
</verification>

<success_criteria>
- kartini page refactored to use shared components
- kartini page line count reduced significantly (from ~931 to ~500-600)
- Shared animation CSS not duplicated on kartini page
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-extract-reusable-masterclass-section-components-and-rebuild-kartini-vinok-pages/04-02-SUMMARY.md`
</output>
