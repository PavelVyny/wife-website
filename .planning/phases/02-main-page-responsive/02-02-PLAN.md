---
phase: 02-main-page-responsive
plan: "02"
type: auto
autonomous: true
---

# Plan 02-02: About, Stats, FAQ, and Footer Desktop Responsiveness

## Objective
Polish the remaining sections of the main page (AboutSection, StatsSection, FaqSection, Footer) for tablet and desktop breakpoints. Ensure consistent layout, spacing, and typography scales across all screen sizes.

## Context
Phase 02-01 covered: max-w-[1280px] standardization, Hero text/image side-by-side on desktop, courses grid 2-col tablet / 3-col desktop.

Remaining sections need desktop polish:
- **AboutSection**: bio text + photo side-by-side layout on desktop already exists but spacing/typography can be improved
- **StatsSection**: already has 4-col desktop grid — verify it renders correctly
- **FaqSection**: already responsive — verify accordion looks good on desktop
- **Footer**: already has responsive layout — verify it works
- **General**: Ensure `reveal` animations and section padding are consistent

## Tasks

### Task 1: Audit and fix AboutSection desktop layout
type: auto

Review AboutSection.astro for any desktop layout issues. The about section uses md:flex for side-by-side layout. Ensure the bio text column is readable at desktop widths with proper max-width constraints. Improve spacing between columns at lg breakpoint. Also ensure the background image positioning is correct on desktop.

Done: AboutSection renders well at md/lg/xl breakpoints with correct proportions.

### Task 2: Audit and fix StatsSection desktop layout
type: auto

Review StatsSection.astro grid (4-col at lg). Verify decorative image positioning doesn't overflow at larger screens. Ensure the stat cards maintain correct aspect and typography scaling at md and lg. Fix any visual issues found.

Done: StatsSection renders correctly at all breakpoints.

### Task 3: Improve FaqSection desktop layout with 2-column grid
type: auto

Currently FaqSection renders as a single column of accordion items at all sizes. On desktop (lg+), split the FAQ items into 2 columns for better space utilization. First 5 items in left column, last 5 in right column. Maintain all existing accordion behavior.

Done: FAQ shows 2-column grid on lg+ screens.

### Task 4: Review and fix page-level spacing on index.astro
type: auto

Review index.astro and ensure section padding/gaps are consistent at desktop widths. The hero section bleeds to the edges; the content sections should have consistent px-8 md padding and max-w-[1280px] centering. Fix any issues found with section separation.

Done: Sections have consistent vertical rhythm at all breakpoints.

## Verification
- Build passes: `pnpm build`
- No TypeScript errors
- All sections visible at 375px, 768px, 1280px widths

## Success Criteria
- [ ] AboutSection looks polished at desktop
- [ ] StatsSection 4-col grid renders correctly
- [ ] FaqSection has 2-column layout at lg+
- [ ] index.astro has consistent section spacing
- [ ] Build passes without errors
