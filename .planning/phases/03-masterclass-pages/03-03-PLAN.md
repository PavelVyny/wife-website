---
phase: "03"
plan: "03"
type: auto
autonomous: true
wave: 1
depends_on: []
requirements: []
---

# Phase 03 Plan 03: Masterclass Detail Pages

## Objective

Create the two masterclass detail pages (`/stvorennya-florariumu` and `/kartini-zi-stabilizovanih-roslin`) using existing shared components.

## Context

Both pages use `MasterclassLayout`, `ValueProps`, `InstructorSection`, and `PricingCta` components that were built in earlier phases. The Navigation already links to these pages. Images are available at `/images/masterclass/`.

## Tasks

### Task 1: Florarium masterclass page

Create `src/pages/stvorennya-florariumu.astro` — the full detail page for the florarium-in-glass-vase masterclass.

**Done criteria:**
- Page builds without errors
- Uses `MasterclassLayout` with correct `currentPath="/stvorennya-florariumu"`
- Has hero section with title, subtitle, and product image
- Uses `ValueProps` with florarium-specific cards
- Uses `InstructorSection`
- Uses `PricingCta` with correct price and purchase URL

### Task 2: Stabilized art masterclass page

Create `src/pages/kartini-zi-stabilizovanih-roslin.astro` — the full detail page for the stabilized plants picture masterclass.

**Done criteria:**
- Page builds without errors
- Uses `MasterclassLayout` with correct `currentPath="/kartini-zi-stabilizovanih-roslin"`
- Has hero section with title, subtitle, and product image
- Uses `ValueProps` with stabilized-art-specific cards
- Uses `InstructorSection`
- Uses `PricingCta` with correct price and purchase URL

### Task 3: Verify both pages build and nav links are correct

Run `pnpm build` to confirm 3 pages build. Confirm nav active state works for each page.

**Done criteria:**
- Build produces 3 pages: `/`, `/stvorennya-florariumu`, `/kartini-zi-stabilizovanih-roslin`
- No TypeScript errors
- No build warnings

## Verification

- `pnpm build` completes with 3 pages
- No errors or TypeScript warnings

## Success Criteria

- Both masterclass pages exist and build successfully
- Pages use consistent components and styling matching the main site
- Nav active highlighting works correctly per page
